# Nova API (required for chat/LLM) — OpenAI-compatible client
NOVA_API_KEY=
NOVA_MODEL_ID=nova-2-pro-v1
NOVA_API_BASE_URL=https://api.nova.amazon.com/v1

# Optional: API base URL for the web app (set at build time)
# NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: SQLite path or RDS URL for the API
# DATABASE_URL=sqlite:///./data/ai_doctor.db

# Optional: Bedrock embeddings for RAG (if using ingest_kb.py)
# BEDROCK_EMBED_MODEL_ID=amazon.titan-embed-text-v2:0
