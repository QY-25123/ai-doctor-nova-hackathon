# Medical knowledge base (for RAG)

Add markdown files here. They will be chunked and embedded by `services/api/scripts/ingest_kb.py`.

## Example

- Use `# Title` for document title (or filename is used).
- Use `## Section` for logical chunks when possible.
- Content is for general health information only; not medical advice.
